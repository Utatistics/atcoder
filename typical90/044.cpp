#include <bits/stdc++.h>

int main () {
    // input processing
    int N, Q:
    std::cin >> N >> Q;

    std::vector<int> A(N);
    std::cin >> A[i];

    // solve
    for (int i = 0; i < Q; i++) {
        int t;
        std::cin >> t;
        if (t == 1)
            continue;
        if (t == 2)
            continue;
        if (t == 3)
            continue;
        
    }

    return 0;

}
