#include <bits/stdc++.h>

using P = std::pair<char, int>;

int main() {
    int T;
    std::cin >> T;

    while (T--) {
        int N;
        std::cin >> N;
        std::string S;
        std::cin >> S;

        /* TODO: cumsum or DP*/

    }

    return 0;
}

