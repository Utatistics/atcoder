#include <bits/stdc++.h>

static const int MAX = 1e9;

int main() {
    int N;
    std::cin >> N;

    std::vector<int> P;
    for (int p = 1;  p <= MAX; p *= 2) {
        P.push_back(p);
    }

    return 0;
}

