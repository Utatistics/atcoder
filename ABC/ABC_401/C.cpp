#include <bits/stdc++.h>

using ll = long long;
static const int MOD = 1e9;

int main() {
    int N, K;
    std::cin >> N >> K;

    std::vector<int> A(N + 1);
    for (int i = 0; i < N; i++) {

    }
    return 0;
}

