#include <bits/stdc++.h>

int main() {
    int N, K;
    std::cin >> N >> K;

    std::vector<int> R(N);
    for (int i = 0; i < N; i++) std::cin >> R[i];

    std::
    auto dfs = [&](int d) {
        if (d == N) return;


    };

    return 0;
}

